// Database credentials extracted from: postgresql://postgres:password@helium/heliumdb?sslmode=disable
// These are stored here for reference but not used since we're using JSON storage

export const dbConfig = {
    user: 'postgres',
    password: 'password',
    host: 'helium',
    database: 'heliumdb',
    sslmode: 'disable'
};

// Original connection string (for reference)
export const originalConnectionString = 'postgresql://postgres:password@helium/heliumdb?sslmode=disable';
